# QR Region Scanner

一个本地运行的 Chrome / Edge 浏览器插件，用来框选当前网页可见区域里的二维码，并返回二维码中的文本或链接。

## 特点

- 二维码识别默认在本地完成，不调用第三方接口
- 二维码识别不上传截图或网页内容
- 点击插件图标后直接框选二维码区域
- 识别成功后可复制结果或打开 `http/https` 链接
- 内置本地 `jsQR` 解码库，兼容不支持 `BarcodeDetector` 的 Edge / Chrome 环境
- 识别结果窗口带 10 秒倒计时并自动关闭
- 本地记录识别历史，重复识别同一内容时给出提示，最多保存 1000 条
- 对浅色、渐变、低对比度二维码进行本地图像增强后重试识别
- 右键插件图标可打开 OCR 配置页，左键仍保留为框选扫描
- 可选接入 OCR.space 在线 OCR，从框选图片文字中提取链接

## 安装

1. 打开 Chrome / Edge 的扩展程序页面。
2. 开启开发者模式。
3. 选择“加载已解压的扩展程序”。
4. 选择本目录。

修改插件文件后，需要在扩展程序页面点击“重新加载”，再回到目标网页刷新页面后使用。

## 使用

1. 打开包含二维码的网页。
2. 点击浏览器工具栏里的插件图标。
3. 拖拽框选二维码。
4. 查看识别结果。

如需使用在线 OCR，可以右键浏览器工具栏里的插件图标，选择“配置 OCR”。

在线 OCR 默认不会自动调用。启用并填写 API Key 后，扫描结果窗口会显示“在线 OCR”按钮；调用时只上传当前框选区域的压缩图片。

## 技术说明

插件使用 Manifest V3：

- `background.js` 负责注入脚本和调用 `chrome.tabs.captureVisibleTab()`。
- `content.js` 负责框选、裁剪截图和本地二维码识别。
- `content.css` 负责框选层和结果面板样式。
- `vendor/jsQR.js` 是本地 vendored 的 `jsQR@1.4.0`，用于兼容 `BarcodeDetector` 不可用的浏览器。

二维码识别流程是：优先尝试浏览器内置 `BarcodeDetector`，不可用或识别失败时自动使用本地 `jsQR`。如果直接识别失败，会继续生成补白边、放大、对比度增强和二值化后的图像版本进行重试。二维码识别过程都在浏览器本地完成，不需要服务端接口。

识别历史使用 `chrome.storage.local` 保存在本机浏览器扩展数据中。同一内容只保存一条记录，并更新首次识别时间、最近识别时间和累计次数；最多保留最近识别的 1000 条记录。

在线 OCR 配置同样保存在 `chrome.storage.local`。启用在线 OCR 后，插件会通过后台脚本请求 `https://api.ocr.space/parse/image`，并从 OCR 返回文本中提取 URL。

## 隐私说明

- 二维码识别、本地图像增强和识别历史记录都在本机浏览器内完成。
- 在线 OCR 是可选功能，默认不会调用；仅在用户启用并点击“在线 OCR”按钮，或启用宽选区自动 OCR 后才会请求 OCR.space。
- 调用在线 OCR 时，插件只上传当前框选区域的压缩图片，不上传完整网页内容。
- OCR API Key 和识别历史保存在本机浏览器扩展数据中。

## 限制

- 只能识别当前屏幕可见区域，二维码不在视口内时需要先滚动到可见位置。
- 浏览器内置页面、扩展商店页面等特殊页面通常不能注入脚本或截图。
- 二维码过小、模糊、遮挡或对比度过低时，识别率会下降。

## 第三方许可

本项目 vendored 了 `jsQR@1.4.0`，许可证见 `vendor/jsQR.LICENSE`。
